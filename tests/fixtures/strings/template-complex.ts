const a = 1;
const b = "hello";
const c = true;
console.log(`a=${a} b=${b} c=${c}`);
console.log(`sum=${a + 5}`);
const arr = [1, 2, 3];
console.log(`first=${arr[0]} len=${arr.length}`);
function fn(): number { return 42; }
console.log(`fn=${fn()}`);
