const arr: number[] = [1, 2, 3, 4, 5];
const sliced = arr.slice(1, 3);
console.log(sliced.length);
console.log(sliced[0]);
console.log(sliced[1]);

const allCopy = arr.slice(0);
console.log(allCopy.length);
