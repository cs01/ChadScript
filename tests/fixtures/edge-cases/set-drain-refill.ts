const s = new Set<string>();
s.add("a");
s.add("b");
s.add("c");
if (s.size !== 3) process.exit(1);
s.delete("a");
s.delete("b");
s.delete("c");
if (s.size !== 0) process.exit(1);
if (s.has("a")) process.exit(1);
s.add("d");
s.add("e");
if (s.size !== 2) process.exit(1);
if (!s.has("d") || !s.has("e")) process.exit(1);
console.log("TEST_PASSED");
