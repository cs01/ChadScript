const words = ["banana", "apple", "cherry"];
console.log(words.sort().join(","));
const defnums = [10, 2, 1, 20, 3];
console.log(defnums.sort().join(","));
const empty: number[] = [];
console.log(empty.sort().join(","));
