---
outline: [2, 2]
---

# Accepted subset

::: info Generated
Everything the compiler accepts, listed from the compiler's own tables by
`scripts/gen-subset.ts` (the page is [`docs/SUBSET.md`](https://github.com/cs01/ChadScript/blob/main/docs/SUBSET.md)).
A test fails if this list and the compiler disagree, so it cannot go stale. Anything not listed
is rejected with a clear error; each error code is explained in the
[error reference](/reference/errors).

Next: [Error codes](/reference/errors) · [CLI](/reference/cli).
:::

<!--@include: ../SUBSET.md{8,}-->
